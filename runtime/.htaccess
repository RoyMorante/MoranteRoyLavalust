<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /

    # Send everything to index.php unless it's an actual file or directory
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php/$1 [L]
</IfModule>
